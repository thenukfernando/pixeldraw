# pixeldraw
Interactive drawing system on DE1-SoC FPGA: webcam tracks a physical marker via OpenCV HSV filtering, streams coordinates over JTAG UART to a NIOS V processor, and renders real-time brush strokes on a VGA display. Features shape detection, rainbow mode, undo buffer, and canvas PNG export. Python + C + FPGA.
